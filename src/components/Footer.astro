---

---

<footer class="site-footer">
    <div class="footer-top">
        <p class="contact-message">
            Let's build something together. 👋 Say hi! I'm always down to talk
            design.
        </p>

        <p class="built-message">
            Designed in Figma. Built with <a
                href="https://astro.build"
                target="_blank"
                rel="noopener noreferrer">Astro</a
            > in <a
                href="https://antigravity.google"
                target="_blank"
                rel="noopener noreferrer">Antigravity</a
            >.
        </p>
    </div>

    <div class="footer-divider"></div>

    <div class="footer-bottom">
        <div class="contact-buttons">
            <button
                class="copy-btn"
                data-clipboard-text="nashanah@andrew.cmu.edu"
            >
                <span class="btn-text">nashanah@andrew.cmu.edu</span>
                <span class="copy-icon">
                    <svg
                        width="12"
                        height="12"
                        viewBox="0 0 24 24"
                        fill="none"
                        stroke="currentColor"
                        stroke-width="2"
                    >
                        <rect x="9" y="9" width="13" height="13" rx="2"></rect>
                        <path
                            d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2-2v1"
                        ></path>
                    </svg>
                </span>
            </button>
            <button class="copy-btn" data-clipboard-text="412-626-2332">
                <span class="btn-text">412-626-2332</span>
                <span class="copy-icon">
                    <svg
                        width="12"
                        height="12"
                        viewBox="0 0 24 24"
                        fill="none"
                        stroke="currentColor"
                        stroke-width="2"
                    >
                        <rect x="9" y="9" width="13" height="13" rx="2"></rect>
                        <path
                            d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2-2v1"
                        ></path>
                    </svg>
                </span>
            </button>
        </div>
        <div class="footer-text">
            <p class="tagline">
                Trying to make cool sh*t for people to fu*k with.
            </p>
            <p class="copyright">
                © {new Date().getFullYear()} Narayan Ashnahalli
            </p>
        </div>
    </div>
</footer>

<script>
    const copyBtns = document.querySelectorAll(".copy-btn");
    copyBtns.forEach((btn) => {
        btn.addEventListener("click", () => {
            const text = btn.getAttribute("data-clipboard-text");
            if (text) {
                navigator.clipboard.writeText(text).then(() => {
                    btn.classList.add("copied");
                    setTimeout(() => {
                        btn.classList.remove("copied");
                    }, 1000);
                });
            }
        });
    });
</script>

<style>
    .site-footer {
        margin-top: var(--space-4xl);
    }

    .footer-top {
        display: flex;
        justify-content: space-between;
        align-items: flex-end;
        margin-bottom: 4px; /* Consistent spacing above divider */
    }

    .contact-message {
        font-size: 14px;
        color: var(--text-secondary);
        margin: 0;
        line-height: 1.4;
        text-align: left;
        max-width: 60%; /* Prevent overlap on smaller desktops */
    }

    .built-message {
        font-size: 14px;
        color: var(--text-secondary);
        margin: 0;
        line-height: 1.4;
        text-align: right;
    }

    .built-message a {
        color: var(--text-primary);
        text-decoration: underline;
        text-underline-offset: 2px;
        opacity: 0.7;
        transition: opacity 0.2s ease;
    }

    .built-message a:hover {
        opacity: 1;
    }

    .footer-divider {
        height: 1px;
        background: var(--border-subtle);
        margin-bottom: 8px;
        width: 100%;
    }

    .footer-bottom {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        gap: 20px;
    }

    .contact-buttons {
        display: flex;
        gap: 8px;
    }

    .copy-btn {
        display: flex;
        align-items: center;
        gap: 8px;
        padding: 0.5rem 0.75rem;
        background: transparent;
        border: 1px solid var(--border-subtle);
        border-radius: var(--radius-md);
        color: var(--text-secondary);
        font-size: 14px;
        font-family: inherit;
        cursor: pointer;
        transition: all 0.15s ease;
        position: relative;
        user-select: none;
    }

    .copy-btn:hover {
        border-color: var(--accent-secondary);
        color: var(--text-primary);
    }

    .copy-btn * {
        cursor: pointer;
    }

    .copy-icon {
        display: flex;
        opacity: 0.5;
    }

    .copy-btn:hover .copy-icon {
        opacity: 1;
    }

    .copy-btn.copied {
        background: var(--accent-secondary);
        border-color: var(--accent-secondary);
        color: white; /* Or a dark color if accent is very bright, but green usually works with white */
        box-shadow:
            0 0 12px rgba(66, 189, 73, 0.4),
            0 0 4px rgba(66, 189, 73, 0.2);
    }

    .footer-text {
        text-align: right;
    }

    .tagline {
        font-size: 14px;
        color: var(--text-secondary);
        margin: 0;
        font-style: italic;
        line-height: 1.3;
    }

    .copyright {
        font-size: 14px;
        color: var(--text-muted);
        margin: 0;
        line-height: 1.3;
    }

    @media (max-width: 600px) {
        .site-footer {
            display: flex;
            flex-direction: column;
            gap: 12px;
        }

        .footer-top,
        .footer-bottom,
        .footer-text {
            display: contents;
        }

        .contact-message {
            order: 1;
            max-width: 100%;
            margin-bottom: 0;
            text-align: left;
        }

        .contact-buttons {
            order: 2;
            flex-wrap: wrap;
        }

        .footer-divider {
            order: 3;
            margin: 4px 0;
        }

        .built-message {
            order: 4;
            text-align: left;
            margin-bottom: 0;
        }

        .tagline {
            display: none;
        }

        .copyright {
            order: 5;
            text-align: left;
        }
    }
</style>
